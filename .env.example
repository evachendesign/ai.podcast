# ======================
# Next.js + Vercel Deployment Environment Variables
# ======================
# Copy this file to .env.local and fill in your actual values.
# Never commit .env.local to Git.

# -----------------------------
# Database (Neon PostgreSQL)
# -----------------------------
DATABASE_URL="postgresql://user:password@host.region.neon.tech/dbname?sslmode=require"

# -----------------------------
# Clerk Authentication
# -----------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."
# Optional: Set if using a custom domain
# CLERK_DOMAIN="your-clerk-domain.clerk.accounts.dev"

# -----------------------------
# Worker API (deployed on Railway)
# -----------------------------
# Production: https://newsagent-production-be4a.up.railway.app
# Development: http://localhost:8000
WORKER_BASE_URL="https://newsagent-production-be4a.up.railway.app"

# Optional: Shared secret for authenticating Next.js → Worker calls
# INTERNAL_SHARED_SECRET="your-shared-secret-here"

# -----------------------------
# AWS S3 (for audio storage)
# -----------------------------
AWS_ACCESS_KEY_ID="AKIA..."
AWS_SECRET_ACCESS_KEY="..."
AWS_REGION="us-east-1"
S3_BUCKET_NAME="your-bucket-name"

# Optional: Public base URL if using CloudFront or custom domain
# S3_PUBLIC_BASE_URL="https://d1234567890.cloudfront.net"

# Optional: Presigned URL TTL in seconds (default: 3600 = 1 hour)
# S3_SIGNED_URL_TTL_SECONDS="3600"

# -----------------------------
# Optional: AI/Tools (if Next.js needs direct access)
# -----------------------------
# Typically, the worker handles these. Only include if Next.js calls OpenAI/Serper directly.
# OPENAI_API_KEY="sk-..."
# SERPER_API_KEY="..."

# -----------------------------
# Optional: Integrations
# -----------------------------
# DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/..."
# TTS_VOICE="alloy"
# TTS_STYLE_CUES="true"

# -----------------------------
# Optional: Server
# -----------------------------
# PORT="3000"
